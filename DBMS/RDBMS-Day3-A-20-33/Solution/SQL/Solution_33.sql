ALTER TABLE EmpDetails ADD (CurrentSalary INTEGER,increased salary INTEGER,incremented amount INTEGER)
UPDATE EmpDetails SET (CurrentSalary = Salary,Increased Salary=1.1*Salary,Incremented Amount=0.1*Salary)
SELECT empid, CurrentSalary, increased salary, incremented amount from EmpDetails
